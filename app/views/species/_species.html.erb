<li>
      
  <%= link_to species.species_name, species %>
  <% if current_staff_member.admin? %>
  | <%= link_to "delete", species, :method => :delete, :confirm => "Are you sure you want to delete this species?",
                                :title => "Delete #{species.species_name}" %>
  <% end %>
    </li>
